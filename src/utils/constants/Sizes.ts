import {moderateScale} from 'react-native-size-matters';

export const Sizes = {
  xs: moderateScale(4),
  sm: moderateScale(8),
  md: moderateScale(16),
  lg: moderateScale(20),
  xl: moderateScale(24),
};
